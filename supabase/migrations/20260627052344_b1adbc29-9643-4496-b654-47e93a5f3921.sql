ALTER TABLE public.daily_articles ADD COLUMN IF NOT EXISTS score integer NOT NULL DEFAULT 0; ALTER TABLE public.daily_articles ADD COLUMN IF NOT EXISTS is_breaking boolean NOT NULL DEFAULT false; CREATE INDEX IF NOT EXISTS daily_articles_score_idx ON public.daily_articles (score DESC, published_at DESC); CREATE INDEX IF NOT EXISTS daily_articles_breaking_idx ON public.daily_articles (is_breaking, published_at DESC);