# Production smoke scripts

These scripts verify live KeepTXRed production contracts that must remain true before an exact verified revision is recorded as current in production.

They are intentionally read-only. They validate public production responses and repository-approved assets; they do not mutate production data.
