import fs from "node:fs";
import path from "node:path";

const files = {
"src/types/relocation/location.ts": `export interface Coordinates { latitude: number; longitude: number; }\nexport interface RelocationLocation { id: string; name: string; city?: string; county?: string; state: "TX"; zipCodes: string[]; coordinates?: Coordinates; slug: string; }\n`,
"src/types/relocation/search.ts": `import type { RelocationLocation } from "./location";\nexport type SearchSortDirection = "asc" | "desc";\nexport interface SearchFilters { city?: string; county?: string; zipCode?: string; [key: string]: string | number | boolean | undefined; }\nexport interface SearchRequest { query: string; filters?: SearchFilters; page?: number; pageSize?: number; sortBy?: string; sortDirection?: SearchSortDirection; }\nexport interface SearchResult<T extends RelocationLocation = RelocationLocation> { item: T; score: number; matchedFields: string[]; distanceMiles?: number; }\nexport interface SearchResponse<T extends RelocationLocation = RelocationLocation> { results: SearchResult<T>[]; total: number; page: number; pageSize: number; }\n`,
"src/types/relocation/dataset.ts": `import type { RelocationLocation } from "./location";\nexport interface RelocationDataset<T extends RelocationLocation> { id: string; name: string; version: string; updatedAt: string; items: T[]; }\nexport interface DatasetIndexEntry { id: string; searchText: string; city?: string; county?: string; zipCodes: string[]; }\n`,
"src/types/relocation/seo.ts": `export interface RelocationSEO { title: string; description: string; canonicalPath: string; keywords?: string[]; noIndex?: boolean; }\n`,
"src/types/relocation/schema.ts": `export type JsonLdValue = string | number | boolean | null | JsonLdObject | JsonLdValue[];\nexport interface JsonLdObject { [key: string]: JsonLdValue; }\n`,
"src/lib/relocation/search/searchNormalizer.ts": `export const normalizeSearchText = (value: string) => value.normalize("NFKD").replace(/[\\u0300-\\u036f]/g, "").toLowerCase().replace(/[^a-z0-9\\s-]/g, " ").replace(/\\s+/g, " ").trim();\nexport const tokenizeSearchText = (value: string) => normalizeSearchText(value).split(" ").filter(Boolean);\n`,
"src/lib/relocation/search/searchRanking.ts": `import { normalizeSearchText, tokenizeSearchText } from "./searchNormalizer";\nexport function rankSearchText(query: string, candidate: string): number { const q=normalizeSearchText(query), c=normalizeSearchText(candidate); if(!q) return 1; if(c===q) return 100; if(c.startsWith(q)) return 75; if(c.includes(q)) return 50; const tokens=tokenizeSearchText(q); return tokens.length ? tokens.filter(token=>c.includes(token)).length/tokens.length*40 : 0; }\n`,
"src/lib/relocation/search/searchEngine.ts": `import type { RelocationLocation } from "@/types/relocation/location";\nimport type { SearchRequest, SearchResponse } from "@/types/relocation/search";\nimport { rankSearchText } from "./searchRanking";\nexport function searchLocations<T extends RelocationLocation>(items:T[], request:SearchRequest):SearchResponse<T>{ const page=Math.max(1,request.page??1), pageSize=Math.min(100,Math.max(1,request.pageSize??20)); const ranked=items.map(item=>({item,score:rankSearchText(request.query,[item.name,item.city,item.county,...item.zipCodes].filter(Boolean).join(" ")),matchedFields:[] as string[]})).filter(row=>row.score>0).filter(({item})=>!request.filters?.city||item.city===request.filters.city).filter(({item})=>!request.filters?.county||item.county===request.filters.county).filter(({item})=>!request.filters?.zipCode||item.zipCodes.includes(String(request.filters.zipCode))).sort((a,b)=>b.score-a.score); return {results:ranked.slice((page-1)*pageSize,page*pageSize),total:ranked.length,page,pageSize}; }\n`,
"src/lib/relocation/indexing/indexTypes.ts": `export interface TextIndex { byToken: Map<string, Set<string>>; allIds: Set<string>; }\n`,
"src/lib/relocation/indexing/datasetIndexer.ts": `import type { DatasetIndexEntry } from "@/types/relocation/dataset";\nimport type { TextIndex } from "./indexTypes";\nimport { tokenizeSearchText } from "../search/searchNormalizer";\nexport function buildTextIndex(entries:DatasetIndexEntry[]):TextIndex{ const byToken=new Map<string,Set<string>>(); for(const entry of entries){ for(const token of tokenizeSearchText(entry.searchText)){ const ids=byToken.get(token)??new Set<string>(); ids.add(entry.id); byToken.set(token,ids); } } return {byToken,allIds:new Set(entries.map(entry=>entry.id))}; }\n`,
"src/lib/relocation/indexing/locationIndex.ts": `import type { RelocationLocation } from "@/types/relocation/location";\nimport type { DatasetIndexEntry } from "@/types/relocation/dataset";\nexport const toLocationIndexEntry=(item:RelocationLocation):DatasetIndexEntry=>({id:item.id,searchText:[item.name,item.city,item.county,...item.zipCodes].filter(Boolean).join(" "),city:item.city,county:item.county,zipCodes:item.zipCodes});\n`,
"src/lib/relocation/seo/relocationSEO.ts": `import type { RelocationSEO } from "@/types/relocation/seo";\nexport const buildRelocationSEO=(input:RelocationSEO):RelocationSEO=>({...input,title:input.title.includes("Keep TX Red")?input.title:\`\${input.title} | Keep TX Red\`});\n`,
"src/lib/relocation/seo/metadata.ts": `import type { RelocationSEO } from "@/types/relocation/seo";\nexport function toMetaTags(seo:RelocationSEO){ return [{title:seo.title},{name:"description",content:seo.description},{name:"robots",content:seo.noIndex?"noindex, nofollow":"index, follow"},{property:"og:title",content:seo.title},{property:"og:description",content:seo.description}]; }\n`,
"src/lib/relocation/schema/jsonLD.ts": `import type { JsonLdObject } from "@/types/relocation/schema";\nexport const serializeJsonLd=(value:JsonLdObject)=>JSON.stringify(value).replace(/</g,"\\u003c");\n`,
"src/lib/relocation/schema/schemaBuilders.ts": `import type { RelocationLocation } from "@/types/relocation/location";\nimport type { JsonLdObject } from "@/types/relocation/schema";\nexport const buildFinderSchema=(name:string,description:string,url:string):JsonLdObject=>({"@context":"https://schema.org","@type":"WebApplication",name,description,url,applicationCategory:"ReferenceApplication",operatingSystem:"Any"});\nexport const buildLocationSchema=(location:RelocationLocation,url:string):JsonLdObject=>({"@context":"https://schema.org","@type":"Place",name:location.name,url,address:{"@type":"PostalAddress",addressLocality:location.city??"",addressRegion:"TX",addressCountry:"US"}});\n`,
"src/lib/relocation/location/locationUtils.ts": `import type { RelocationLocation } from "@/types/relocation/location";\nexport const locationLabel=(location:RelocationLocation)=>[location.city,location.county?\`\${location.county} County\`:undefined,"Texas"].filter(Boolean).join(", ");\nexport const locationMatches=(location:RelocationLocation,value:string)=>[location.name,location.city,location.county,...location.zipCodes].filter(Boolean).some(part=>String(part).toLowerCase()===value.toLowerCase());\n`,
"src/lib/relocation/location/zipUtils.ts": `export const normalizeTexasZip=(value:string)=>value.replace(/\\D/g,"").slice(0,5);\nexport const isValidTexasZip=(value:string)=>/^(7[3-9]|8[0-8])\\d{3}$/.test(normalizeTexasZip(value));\n`,
"src/lib/relocation/location/countyUtils.ts": `export const normalizeCountyName=(value:string)=>value.trim().replace(/\\s+county$/i,"").replace(/\\b\\w/g,char=>char.toUpperCase());\nexport const countySlug=(value:string)=>normalizeCountyName(value).toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");\n`,
"src/lib/relocation/distance/haversine.ts": `import type { Coordinates } from "@/types/relocation/location";\n+const radians=(degrees:number)=>degrees*Math.PI/180;\n+export function haversineMiles(a:Coordinates,b:Coordinates){ const earth=3958.8,dLat=radians(b.latitude-a.latitude),dLon=radians(b.longitude-a.longitude); const x=Math.sin(dLat/2)**2+Math.cos(radians(a.latitude))*Math.cos(radians(b.latitude))*Math.sin(dLon/2)**2; return earth*2*Math.atan2(Math.sqrt(x),Math.sqrt(1-x)); }\n`.replace(/^\+/gm,""),
"src/lib/relocation/distance/nearby.ts": `import type { Coordinates, RelocationLocation } from "@/types/relocation/location";\nimport { haversineMiles } from "./haversine";\nexport function findNearby<T extends RelocationLocation>(items:T[],origin:Coordinates,limit=10,maxMiles=100){ return items.filter(item=>item.coordinates).map(item=>({item,distanceMiles:haversineMiles(origin,item.coordinates!)})).filter(row=>row.distanceMiles<=maxMiles).sort((a,b)=>a.distanceMiles-b.distanceMiles).slice(0,limit); }\n`,
"src/lib/relocation/validation/validators.ts": `export const isNonEmptyString=(value:unknown):value is string=>typeof value==="string"&&value.trim().length>0;\nexport const isFiniteNumber=(value:unknown):value is number=>typeof value==="number"&&Number.isFinite(value);\nexport const isCoordinate=(latitude:unknown,longitude:unknown)=>isFiniteNumber(latitude)&&isFiniteNumber(longitude)&&latitude>=-90&&latitude<=90&&longitude>=-180&&longitude<=180;\n`,
"src/lib/relocation/validation/relocationValidation.ts": `import type { RelocationLocation } from "@/types/relocation/location";\nimport { isCoordinate, isNonEmptyString } from "./validators";\nexport function validateRelocationLocation(location:RelocationLocation):string[]{ const errors:string[]=[]; if(!isNonEmptyString(location.id))errors.push("id is required"); if(!isNonEmptyString(location.name))errors.push("name is required"); if(!isNonEmptyString(location.slug))errors.push("slug is required"); if(location.state!=="TX")errors.push("state must be TX"); if(location.coordinates&&!isCoordinate(location.coordinates.latitude,location.coordinates.longitude))errors.push("coordinates are invalid"); return errors; }\n`,
"src/types/schools/SchoolDistrict.ts": `import type { RelocationLocation } from "@/types/relocation/location";\nexport interface SchoolDistrict extends RelocationLocation { districtId:string; districtType:"independent"|"consolidated"|"common"; website?:string; phone?:string; enrollment?:number; accountabilityRating?:"A"|"B"|"C"|"D"|"F"|"Not Rated"; gradeSpan?:string; }\n`,
"src/types/schools/Campus.ts": `import type { Coordinates } from "@/types/relocation/location";\nexport interface Campus { id:string; districtId:string; name:string; campusType:"elementary"|"middle"|"high"|"other"; city:string; zipCode:string; coordinates?:Coordinates; enrollment?:number; accountabilityRating?:"A"|"B"|"C"|"D"|"F"|"Not Rated"; }\n`,
"src/types/schools/SchoolSearch.ts": `import type { SearchRequest, SearchResponse } from "@/types/relocation/search";\nimport type { SchoolDistrict } from "./SchoolDistrict";\nexport interface SchoolSearchFilters { city?:string; county?:string; zipCode?:string; minimumRating?:"A"|"B"|"C"|"D"|"F"; }\nexport interface SchoolSearchRequest extends Omit<SearchRequest,"filters"> { filters?:SchoolSearchFilters; }\nexport type SchoolSearchResponse=SearchResponse<SchoolDistrict>;\n`,
"src/data/schools/texasSchoolDistricts.ts": `import type { SchoolDistrict } from "@/types/schools/SchoolDistrict";\nexport const texasSchoolDistricts:SchoolDistrict[]=[{id:"katy-isd",districtId:"101914",name:"Katy Independent School District",districtType:"independent",city:"Katy",county:"Harris",state:"TX",zipCodes:["77449","77450","77493","77494"],slug:"katy-isd",website:"https://www.katyisd.org",gradeSpan:"PK-12"},{id:"houston-isd",districtId:"101912",name:"Houston Independent School District",districtType:"independent",city:"Houston",county:"Harris",state:"TX",zipCodes:["77002","77004","77005","77006"],slug:"houston-isd",website:"https://www.houstonisd.org",gradeSpan:"PK-12"}];\n`,
"src/data/schools/schoolDistrictIndexes.ts": `import { texasSchoolDistricts } from "./texasSchoolDistricts";\nexport const schoolDistrictById=new Map(texasSchoolDistricts.map(district=>[district.id,district]));\nexport const schoolDistrictsByCounty=Map.groupBy(texasSchoolDistricts,district=>district.county??"");\n`,
"src/lib/schools/schoolSearch.ts": `import { texasSchoolDistricts } from "@/data/schools/texasSchoolDistricts";\nimport { searchLocations } from "@/lib/relocation/search/searchEngine";\nimport type { SchoolSearchRequest, SchoolSearchResponse } from "@/types/schools/SchoolSearch";\nexport const searchSchoolDistricts=(request:SchoolSearchRequest):SchoolSearchResponse=>searchLocations(texasSchoolDistricts,{...request,filters:request.filters});\n`,
"src/lib/schools/schoolFilterConfig.ts": `export const schoolFilterConfig=[{key:"county",label:"County",type:"text" as const},{key:"city",label:"City",type:"text" as const},{key:"zipCode",label:"ZIP code",type:"text" as const},{key:"minimumRating",label:"Minimum rating",type:"select" as const,options:["A","B","C","D","F"]}];\n`,
"src/lib/schools/schoolSEO.ts": `import { buildRelocationSEO } from "@/lib/relocation/seo/relocationSEO";\nexport const schoolFinderSEO=buildRelocationSEO({title:"Find My Texas School District",description:"Search Texas school districts by district name, city, county, or ZIP code.",canonicalPath:"/find-my-school-district",keywords:["Texas school district finder","find my ISD"]});\n`,
"src/lib/schools/schoolSchemas.ts": `import { buildFinderSchema } from "@/lib/relocation/schema/schemaBuilders";\nexport const schoolFinderSchema=buildFinderSchema("Texas School District Finder","Find Texas public school districts by city, county, ZIP code, or district name.","https://www.keeptxred.com/find-my-school-district");\n`,
};

const componentFiles = {
"src/components/relocation/SearchBox.tsx": `import type { FormEvent } from "react";\nexport default function SearchBox({value,onChange,onSubmit,placeholder="Search by name, city, county, or ZIP"}:{value:string;onChange:(value:string)=>void;onSubmit?:()=>void;placeholder?:string}){const submit=(event:FormEvent)=>{event.preventDefault();onSubmit?.()};return <form onSubmit={submit} className="flex gap-2"><label className="sr-only" htmlFor="relocation-search">Search</label><input id="relocation-search" className="min-w-0 flex-1 rounded-md border bg-background px-3 py-2" value={value} onChange={event=>onChange(event.target.value)} placeholder={placeholder}/><button className="rounded-md bg-primary px-4 py-2 font-semibold text-primary-foreground" type="submit">Search</button></form>}\n`,
"src/components/relocation/FilterPanel.tsx": `import type { ReactNode } from "react"; export default function FilterPanel({title="Filters",children,onClear}:{title?:string;children:ReactNode;onClear?:()=>void}){return <aside className="rounded-xl border bg-card p-4"><div className="mb-4 flex items-center justify-between"><h2 className="font-semibold">{title}</h2>{onClear&&<button onClick={onClear} className="text-sm underline">Clear</button>}</div><div className="space-y-4">{children}</div></aside>}\n`,
"src/components/relocation/ResultCard.tsx": `import type { ReactNode } from "react"; export default function ResultCard({title,subtitle,children,href}:{title:string;subtitle?:string;children?:ReactNode;href?:string}){const content=<div className="rounded-xl border bg-card p-5 shadow-sm"><h3 className="text-lg font-semibold">{title}</h3>{subtitle&&<p className="mt-1 text-sm text-muted-foreground">{subtitle}</p>}{children&&<div className="mt-4">{children}</div>}</div>;return href?<a className="block transition hover:-translate-y-0.5" href={href}>{content}</a>:content}\n`,
"src/components/relocation/LocationHeader.tsx": `export default function LocationHeader({title,location,description}:{title:string;location?:string;description?:string}){return <header className="space-y-2"><p className="text-sm font-semibold uppercase tracking-wider text-primary">{location??"Texas"}</p><h1 className="text-3xl font-bold tracking-tight md:text-4xl">{title}</h1>{description&&<p className="max-w-3xl text-muted-foreground">{description}</p>}</header>}\n`,
"src/components/relocation/NearbyResults.tsx": `import ResultCard from "./ResultCard"; export default function NearbyResults({title="Nearby results",items}:{title?:string;items:Array<{id:string;name:string;distanceMiles?:number;href?:string}>}){if(!items.length)return null;return <section><h2 className="mb-4 text-xl font-bold">{title}</h2><div className="grid gap-4 md:grid-cols-2">{items.map(item=><ResultCard key={item.id} title={item.name} subtitle={item.distanceMiles===undefined?undefined:\`\${item.distanceMiles.toFixed(1)} miles away\`} href={item.href}/>)}</div></section>}\n`,
"src/components/relocation/ShareResults.tsx": `export default function ShareResults({title,url}:{title:string;url?:string}){const share=async()=>{const target=url??window.location.href;if(navigator.share)await navigator.share({title,url:target});else await navigator.clipboard.writeText(target)};return <button type="button" onClick={()=>void share()} className="rounded-md border px-4 py-2 text-sm font-semibold">Share results</button>}\n`,
"src/components/relocation/LoadingState.tsx": `export default function LoadingState({label="Loading results…"}:{label?:string}){return <div role="status" aria-live="polite" className="rounded-xl border p-8 text-center text-muted-foreground"><span className="inline-block size-5 animate-spin rounded-full border-2 border-current border-t-transparent"/> <span>{label}</span></div>}\n`,
"src/components/relocation/EmptyState.tsx": `import type { ReactNode } from "react"; export default function EmptyState({title="No results found",description="Try a different search or remove a filter.",action}:{title?:string;description?:string;action?:ReactNode}){return <div className="rounded-xl border border-dashed p-8 text-center"><h2 className="font-semibold">{title}</h2><p className="mt-2 text-sm text-muted-foreground">{description}</p>{action&&<div className="mt-4">{action}</div>}</div>}\n`,
"src/components/relocation/ResultList.tsx": `import type { ReactNode } from "react"; import EmptyState from "./EmptyState"; export default function ResultList<T>({items,renderItem,getKey,emptyMessage}:{items:T[];renderItem:(item:T,index:number)=>ReactNode;getKey:(item:T)=>string;emptyMessage?:string}){if(!items.length)return <EmptyState description={emptyMessage}/>;return <div className="grid gap-4">{items.map((item,index)=><div key={getKey(item)}>{renderItem(item,index)}</div>)}</div>}\n`,
"src/components/relocation/Pagination.tsx": `export default function Pagination({page,totalPages,onPageChange}:{page:number;totalPages:number;onPageChange:(page:number)=>void}){if(totalPages<=1)return null;return <nav aria-label="Results pages" className="flex items-center justify-center gap-3"><button disabled={page<=1} onClick={()=>onPageChange(page-1)} className="rounded border px-3 py-2 disabled:opacity-50">Previous</button><span className="text-sm">Page {page} of {totalPages}</span><button disabled={page>=totalPages} onClick={()=>onPageChange(page+1)} className="rounded border px-3 py-2 disabled:opacity-50">Next</button></nav>}\n`,
"src/components/relocation/SortSelector.tsx": `export default function SortSelector({value,onChange,options}:{value:string;onChange:(value:string)=>void;options:Array<{value:string;label:string}>}){return <label className="flex items-center gap-2 text-sm">Sort <select className="rounded border bg-background px-3 py-2" value={value} onChange={event=>onChange(event.target.value)}>{options.map(option=><option key={option.value} value={option.value}>{option.label}</option>)}</select></label>}\n`,
"src/components/relocation/SearchToolbar.tsx": `import type { ReactNode } from "react"; export default function SearchToolbar({resultCount,children}:{resultCount:number;children?:ReactNode}){return <div className="flex flex-wrap items-center justify-between gap-3 border-y py-3"><p className="text-sm text-muted-foreground">{resultCount.toLocaleString()} result{resultCount===1?"":"s"}</p><div className="flex items-center gap-3">{children}</div></div>}\n`,
"src/components/schools/SchoolDistrictCard.tsx": `import ResultCard from "@/components/relocation/ResultCard"; import type { SchoolDistrict } from "@/types/schools/SchoolDistrict"; export default function SchoolDistrictCard({district}:{district:SchoolDistrict}){return <ResultCard title={district.name} subtitle={[district.city,district.county&&\`\${district.county} County\`].filter(Boolean).join(", ")} href={\`/find-my-school-district?district=\${district.slug}\`}><dl className="grid grid-cols-2 gap-2 text-sm"><div><dt className="text-muted-foreground">Grades</dt><dd>{district.gradeSpan??"Not reported"}</dd></div><div><dt className="text-muted-foreground">Rating</dt><dd>{district.accountabilityRating??"Not rated"}</dd></div></dl></ResultCard>}\n`,
"src/components/schools/SchoolResultRenderer.tsx": `import ResultList from "@/components/relocation/ResultList"; import type { SearchResult } from "@/types/relocation/search"; import type { SchoolDistrict } from "@/types/schools/SchoolDistrict"; import SchoolDistrictCard from "./SchoolDistrictCard"; export default function SchoolResultRenderer({results}:{results:Array<SearchResult<SchoolDistrict>>}){return <ResultList items={results} getKey={result=>result.item.id} renderItem={result=><SchoolDistrictCard district={result.item}/>} emptyMessage="No matching Texas school districts were found."/>}\n`,
};

Object.assign(files, componentFiles);
for (const [file, content] of Object.entries(files)) { fs.mkdirSync(path.dirname(file), { recursive:true }); if (!fs.existsSync(file)) fs.writeFileSync(file, content); }
