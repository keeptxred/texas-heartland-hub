#!/usr/bin/env node

// Protected current-main replay marker after direct post-verify dispatch was installed.
// This file intentionally applies no portrait findings by itself. Its candidate-photo
// input path causes the trusted auto-merge workflow to dispatch the existing full
// candidate-photo enrichment workflow only after exact current-main verify succeeds.
// All provenance, rights, generic-image, eligibility, and branch-protection gates remain unchanged.

console.log("Candidate-photo direct-dispatch replay marker: no direct findings applied.");
