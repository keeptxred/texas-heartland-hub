Change the `<title>` meta entry in `src/routes/tax-calculator.tsx` from:

"Texas Property Tax Relief Calculator 2026 — Estimate Your Tax Bill & Savings"

to:

"Texas Property Tax Relief Calculator 2026 | Estimate & Save"

Scope: single-line edit to the `title` meta tag only. No changes to page content, calculator logic, schema markup, headings, URLs, internal links, or Open Graph tags.