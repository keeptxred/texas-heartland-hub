name: Daily News Production Test (inactive)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inactive:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'One-time production test is inactive.'
