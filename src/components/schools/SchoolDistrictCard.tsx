import ResultCard from "@/components/relocation/ResultCard"; import type { SchoolDistrict } from "@/types/schools/SchoolDistrict"; export default function SchoolDistrictCard({district}:{district:SchoolDistrict}){return <ResultCard title={district.name} subtitle={[district.city,district.county&&`${district.county} County`].filter(Boolean).join(", ")} href={`/find-my-school-district?district=${district.slug}`}><dl className="grid grid-cols-2 gap-2 text-sm"><div><dt className="text-muted-foreground">Grades</dt><dd>{district.gradeSpan??"Not reported"}</dd></div><div><dt className="text-muted-foreground">Rating</dt><dd>{district.accountabilityRating??"Not rated"}</dd></div></dl></ResultCard>}
