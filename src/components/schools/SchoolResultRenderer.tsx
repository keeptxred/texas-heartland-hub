import ResultList from "@/components/relocation/ResultList"; import type { SearchResult } from "@/types/relocation/search"; import type { SchoolDistrict } from "@/types/schools/SchoolDistrict"; import SchoolDistrictCard from "./SchoolDistrictCard"; export default function SchoolResultRenderer({results}:{results:Array<SearchResult<SchoolDistrict>>}){return <ResultList items={results} getKey={result=>result.item.id} renderItem={result=><SchoolDistrictCard district={result.item}/>} emptyMessage="No matching Texas school districts were found."/>}
