import type { ReactNode } from "react"; export default function SearchToolbar({resultCount,children}:{resultCount:number;children?:ReactNode}){return <div className="flex flex-wrap items-center justify-between gap-3 border-y py-3"><p className="text-sm text-muted-foreground">{resultCount.toLocaleString()} result{resultCount===1?"":"s"}</p><div className="flex items-center gap-3">{children}</div></div>}
