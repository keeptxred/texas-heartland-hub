import type { ReactNode } from "react"; export default function FilterPanel({title="Filters",children,onClear}:{title?:string;children:ReactNode;onClear?:()=>void}){return <aside className="rounded-xl border bg-card p-4"><div className="mb-4 flex items-center justify-between"><h2 className="font-semibold">{title}</h2>{onClear&&<button onClick={onClear} className="text-sm underline">Clear</button>}</div><div className="space-y-4">{children}</div></aside>}
