export default function ShareResults({title,url}:{title:string;url?:string}){const share=async()=>{const target=url??window.location.href;if(navigator.share)await navigator.share({title,url:target});else await navigator.clipboard.writeText(target)};return <button type="button" onClick={()=>void share()} className="rounded-md border px-4 py-2 text-sm font-semibold">Share results</button>}
