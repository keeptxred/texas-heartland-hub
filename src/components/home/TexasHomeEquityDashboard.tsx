import {useState} from "react";
import {analyzeTexasHomeEquity} from "@/lib/home/texasHomeEquityEngine";
import {formatEquityCurrency,explainEquityScore} from "@/lib/home/texasHomeEquityHelpers";
export default function TexasHomeEquityDashboard(){const[homeValue,setHomeValue]=useState(500000),[mortgageBalance,setMortgageBalance]=useState(300000),[purchasePrice,setPurchasePrice]=useState(400000),[appreciation,setAppreciation]=useState(.03),[result,setResult]=useState<ReturnType<typeof analyzeTexasHomeEquity>|null>(null);const calculate=()=>setResult(analyzeTexasHomeEquity({homeValue,mortgageBalance,originalPurchasePrice:purchasePrice,annualAppreciationRate:appreciation,yearsOwned:5,maximumLoanToValue:.8}));return <div className="space-y-8"><div className="grid gap-5 md:grid-cols-2"><label>Current Home Value<input type="number" value={homeValue} onChange={e=>setHomeValue(Number(e.target.value))} className="w-full rounded-lg border p-3"/></label><label>Mortgage Balance<input type="number" value={mortgageBalance} onChange={e=>setMortgageBalance(Number(e.target.value))} className="w-full rounded-lg border p-3"/></label><label>Original Purchase Price<input type="number" value={purchasePrice} onChange={e=>setPurchasePrice(Number(e.target.value))} className="w-full rounded-lg border p-3"/></label><label>Annual Appreciation Rate<input type="number" step="0.01" value={appreciation} onChange={e=>setAppreciation(Number(e.target.value))} className="w-full rounded-lg border p-3"/></label></div><button onClick={calculate} className="rounded-lg px-6 py-3 font-semibold">Calculate Home Equity</button>{result&&<div className="space-y-4 rounded-xl border p-6"><h2 className="text-2xl font-bold">Home Equity Score: {result.decisionScore}/100</h2><p>{explainEquityScore(result)}</p><p>Current equity: <strong>{formatEquityCurrency(result.currentEquity.totalEquity)}</strong></p><p>Available equity: <strong>{formatEquityCurrency(result.equityAccess.availableEquity)}</strong></p></div>}</div>}
