import {useState} from "react";
import {analyzeTexasHomeAffordability} from "@/lib/homeAffordability/texasHomeAffordabilityEngine";
import {formatAffordabilityCurrency,calculateBuyerReadinessScore} from "@/lib/homeAffordability/texasHomeAffordabilityHelpers";
export default function TexasHomeAffordabilityCalculator(){const[income,setIncome]=useState(120000),[debts,setDebts]=useState(800),[downPayment,setDownPayment]=useState(50000),[rate,setRate]=useState(.065),[result,setResult]=useState<ReturnType<typeof analyzeTexasHomeAffordability>|null>(null);const calculate=()=>setResult(analyzeTexasHomeAffordability({buyer:{annualIncome:income,monthlyDebtPayments:debts,downPayment,creditScore:720,interestRate:rate,loanTermYears:30,propertyTaxRate:.018,annualInsurance:3600,hoaMonthly:100}}));return <div className="space-y-8"><div className="grid gap-5 md:grid-cols-2"><label>Annual Income<input type="number" value={income} onChange={e=>setIncome(Number(e.target.value))} className="w-full rounded-lg border p-3"/></label><label>Monthly Debt Payments<input type="number" value={debts} onChange={e=>setDebts(Number(e.target.value))} className="w-full rounded-lg border p-3"/></label><label>Down Payment<input type="number" value={downPayment} onChange={e=>setDownPayment(Number(e.target.value))} className="w-full rounded-lg border p-3"/></label><label>Interest Rate<input type="number" step="0.001" value={rate} onChange={e=>setRate(Number(e.target.value))} className="w-full rounded-lg border p-3"/></label></div><button onClick={calculate} className="rounded-lg px-6 py-3 font-semibold">Calculate Home Affordability</button>{result&&<div className="space-y-4 rounded-xl border p-6"><h2 className="text-2xl font-bold">Estimated Affordable Home Price</h2><p className="text-4xl font-bold">{formatAffordabilityCurrency(result.homePriceAnalysis.maximumHomePrice)}</p><p>Comfortable range: {formatAffordabilityCurrency(result.homePriceAnalysis.comfortableHomePrice)}</p><p>Readiness: {calculateBuyerReadinessScore(result)}/100</p></div>}</div>}
