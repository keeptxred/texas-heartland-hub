declare module 'cloudflare:workers' {
  /**
   * Runtime bindings exposed by the Cloudflare Workers Vite plugin.
   * Concrete binding shapes are narrowed at each call site.
   */
  export const env: unknown
}
