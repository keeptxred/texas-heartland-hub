import type {TexasHomeAffordabilityInput,TexasHomeAffordabilityResult} from "@/types/homeAffordability/TexasHomeAffordability";
import {affordabilityDTIDefaults} from "@/data/homeAffordability/texasHomeAffordabilityDefaults";
function loanFromPayment(payment:number,rate:number,years:number){const r=rate/12,n=years*12;return r===0?payment*n:payment*(Math.pow(1+r,n)-1)/(r*Math.pow(1+r,n));}
export function analyzeTexasHomeAffordability(input:TexasHomeAffordabilityInput):TexasHomeAffordabilityResult{const income=input.buyer.annualIncome/12,maxPay=income*affordabilityDTIDefaults.lenderHousingRatio,comfortablePay=income*affordabilityDTIDefaults.comfortableHousingRatio,currentDebtRatio=income?input.buyer.monthlyDebtPayments/income:0,futureDebtRatio=income?(input.buyer.monthlyDebtPayments+maxPay)/income:0,loanAmount=loanFromPayment(maxPay,input.buyer.interestRate,input.buyer.loanTermYears),comfortableLoan=loanFromPayment(comfortablePay,input.buyer.interestRate,input.buyer.loanTermYears),maximumHomePrice=loanAmount+input.buyer.downPayment,comfortableHomePrice=comfortableLoan+input.buyer.downPayment;let score=100;if(futureDebtRatio>.43)score-=25;if(futureDebtRatio>.5)score-=25;if(currentDebtRatio>.2)score-=10;const rating=score>=85?"excellent":score>=70?"comfortable":score>=50?"stretch":"high_risk";return{input,paymentAnalysis:{grossMonthlyIncome:income,maximumHousingPayment:maxPay,comfortableHousingPayment:comfortablePay,currentDebtRatio,futureDebtRatio},homePriceAnalysis:{maximumHomePrice,comfortableHomePrice,loanAmount,downPayment:input.buyer.downPayment},score:{score,rating},recommendation:`Estimated comfortable home price: $${Math.round(comfortableHomePrice).toLocaleString("en-US")}.`};}
