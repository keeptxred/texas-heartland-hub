import type {TexasMortgagePayoffInput,TexasMortgagePayoffResult} from "@/types/mortgage/TexasMortgagePayoff";
function schedule(balance:number,rate:number,payment:number,extra=0,annualExtra=0){const monthlyRate=rate/12;let remaining=balance,months=0,totalPaid=0,totalInterest=0;while(remaining>0&&months<600){const interest=remaining*monthlyRate;let principal=payment+extra-interest;if((months+1)%12===0)principal+=annualExtra;if(principal<=0)break;if(principal>remaining)principal=remaining;remaining-=principal;totalPaid+=principal+interest;totalInterest+=interest;months++;}return{months,totalPaid,totalInterest}}
export function analyzeTexasMortgagePayoff(input:TexasMortgagePayoffInput):TexasMortgagePayoffResult{const base=schedule(input.currentBalance,input.interestRate,input.currentMonthlyPayment),acceleratedSchedule=schedule(input.currentBalance,input.interestRate,input.currentMonthlyPayment,input.strategy==="monthly_extra"?input.extraMonthlyPayment:0,input.strategy==="annual_lump_sum"?input.annualExtraPayment:0),monthsSaved=base.months-acceleratedSchedule.months,yearsSaved=monthsSaved/12,currentYear=new Date().getFullYear();return{input,baseline:{remainingMonths:base.months,remainingYears:base.months/12,totalRemainingPayments:base.totalPaid,remainingInterest:base.totalInterest},accelerated:{newPayoffMonths:acceleratedSchedule.months,newPayoffYears:acceleratedSchedule.months/12,monthsSaved,yearsSaved,newTotalPayments:acceleratedSchedule.totalPaid,interestSaved:base.totalInterest-acceleratedSchedule.totalInterest},paymentComparison:{currentPayment:input.currentMonthlyPayment,acceleratedPayment:input.currentMonthlyPayment+input.extraMonthlyPayment,monthlyDifference:input.extraMonthlyPayment},freedomTimeline:{currentPayoffYear:currentYear+Math.ceil(base.months/12),acceleratedPayoffYear:currentYear+Math.ceil(acceleratedSchedule.months/12),yearsUntilFreedom:acceleratedSchedule.months/12},recommendation:yearsSaved>=10?"This strategy could dramatically accelerate mortgage freedom.":yearsSaved>=5?"This strategy creates meaningful mortgage acceleration and savings.":yearsSaved>=2?"Additional principal payments can shorten your mortgage timeline.":"This strategy creates limited payoff acceleration."};}
